VITE_API_URL=http://localhost:8080
# Para build destinado a staging/produção, use a URL HTTPS da API correspondente.
# VITE_* é público e incorporado ao bundle; nunca colocar senhas/tokens privados.
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
# Identificador do commit; Vercel preenche automaticamente no build quando disponível.
VITE_APP_VERSION=local
